% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-unique-values.R
\name{get_unique_values}
\alias{get_unique_values}
\title{Get unique value counts per forecast unit column}
\usage{
get_unique_values(forecast, by = NULL)
}
\arguments{
\item{forecast}{A forecast object (a validated data.table with predicted and
observed values).}

\item{by}{character vector (default: \code{NULL}). If specified, results are
grouped by this column, showing unique value counts for each forecast
unit column per group. The grouping column itself is excluded from the
column-level counts.}
}
\value{
A data.table with columns \code{"column"} (the name of the forecast unit
column) and \code{"N_unique"} (the number of unique values in that column).
If \code{by} is specified, there is an additional column for the grouping
variable.
}
\description{
Given a forecast object, this function counts the number of unique values
in each column that defines the forecast unit (as determined by
\code{\link[=get_forecast_unit]{get_forecast_unit()}}). This is useful for getting a quick overview of the
structure and scope of a forecast dataset.
}
\examples{
\dontshow{
  data.table::setDTthreads(2) # restricts number of cores used on CRAN
}

example_quantile |>
  as_forecast_quantile() |>
  get_unique_values()

example_quantile |>
  as_forecast_quantile() |>
  get_unique_values(by = "model")
}
\keyword{gain-insights}
